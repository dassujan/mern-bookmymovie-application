// Import the React object from the 'react' module.
import React from "react";

// Define a new functional component called Login.
function Login() {
  // Return JSX representing the component's output.
  // In this case, it returns a <div> element with the content "Login" inside it.
  return <div>Login</div>;
}

// Export the Login component as the default export of this file.
// This allows other files in the application to import and use this component.
export default Login;