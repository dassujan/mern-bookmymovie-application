// Import the React object from the 'react' module.
import React from "react";

// Define a function component called 'Home'.
function Home() {
  // Return the JSX element representing the content of the Home component.
  // In this case, it's a <div> element with the text "Home" inside it.
  return <div>Home</div>;
}

// Export the Home component as the default export of this module.
// This allows other parts of the application to import and use this component without specifying its name.
export default Home;